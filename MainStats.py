import CoathMaker
import CoOrgMaker
import CoCountyMaker

if __name__ == "__main__":
    plst = getfiles()
    mlst = bin(pslt)
    coathlst = coauth(mlst)
    coContry = coCont(mlst)
    coOrg = coOg(mlst)
    for stast on newtwokx:
        run
        goto 1
